name: test
on: 
  push:
   branches:
     - main

jobs:
  build:
    test_runner:
        runs-on: [self-hosted]
        steps:
        - name: get-env
          run: env
          shell: bash
          working-directory: /home/jkibari3/test
