name: test
on: 
  push:
   branches:
     - main

jobs:
    test_runner:
        runs-on: self-hosted
        env:
        steps:
        - name: get-env
          run: env
          working-directory: /jkibari2/test
