name: test
on: 
  push:
   branches:
     - main

jobs:
    test_runner:
        runs-on: [self-hosted]
        steps:
        - name: get-env
          run: env
     defaults:
      run:
       shell: bash
       working-directory: /home/jkibari3/test
