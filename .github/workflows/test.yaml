name: test
on: 
  push:
   branches:
     - main

jobs:
    test_runner:
        runs-on: [self-hosted]
        steps:
        - name: get-env
          run: env
          shell: bash
          working-directory: jkibari3/test
